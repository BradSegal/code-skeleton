# Agentic workflow fixture

`normalize` and `prepare` deliberately share structure while retaining an
unsettled naming and ownership distinction. Consumers must review the evidence
before choosing whether to keep, consolidate, generalise, or separate them.
