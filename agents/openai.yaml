interface:
  display_name: "Anatomize"
  short_description: "Find focused evidence for a code task"
  default_prompt: "Use $anatomize to map this repository, find the files, callers, tests, and documentation relevant to my task, and report any evidence gaps before editing."
